diff --git a/src/components/command/CriticalAlertCard.tsx b/src/components/command/CriticalAlertCard.tsx
index 41f2233..b4a3d3a 100644
--- a/src/components/command/CriticalAlertCard.tsx
+++ b/src/components/command/CriticalAlertCard.tsx
@@ -1,7 +1,8 @@
-import { fleet } from "@/data/fleet";
+import { useFleet } from "@/context/FleetContext";
 import { calculateAlerts } from "@/engine/alertEngine";
 
 export default function CriticalAlertCard() {
+  const { fleet } = useFleet();
 
   const alerts = fleet.flatMap(ship =>
     calculateAlerts(ship)
diff --git a/src/components/command/FleetHeatMap.tsx b/src/components/command/FleetHeatMap.tsx
index bcc0d66..763d763 100644
--- a/src/components/command/FleetHeatMap.tsx
+++ b/src/components/command/FleetHeatMap.tsx
@@ -1,7 +1,8 @@
-import { fleet } from "@/data/fleet";
+import { useFleet } from "@/context/FleetContext";
 import { calculateReadiness } from "@/engine/calculateReadiness";
 
 export default function FleetHeatMap() {
+  const { fleet } = useFleet();
 
   return (
     <div className="rounded-2xl border border-slate-800 bg-slate-950/70 p-6">
diff --git a/src/components/command/FleetOverviewCard.tsx b/src/components/command/FleetOverviewCard.tsx
index 51578d6..25f4a84 100644
--- a/src/components/command/FleetOverviewCard.tsx
+++ b/src/components/command/FleetOverviewCard.tsx
@@ -1,6 +1,7 @@
-import { fleet } from "@/data/fleet";
+import { useFleet } from "@/context/FleetContext";
 
 export default function FleetOverviewCard() {
+  const { fleet } = useFleet();
 
   const ready = fleet.filter((s) => s.readiness === "Y").length;
   const limited = fleet.filter((s) => s.readiness === "Q").length;
diff --git a/src/components/command/ImmediateActionCard.tsx b/src/components/command/ImmediateActionCard.tsx
index f4649e8..0df5ada 100644
--- a/src/components/command/ImmediateActionCard.tsx
+++ b/src/components/command/ImmediateActionCard.tsx
@@ -1,7 +1,8 @@
-import { fleet } from "@/data/fleet";
+import { useFleet } from "@/context/FleetContext";
 import { calculateActions } from "@/engine/actionEngine";
 
 export default function ImmediateActionCard() {
+  const { fleet } = useFleet();
 
   const actions = fleet.flatMap(ship =>
     calculateActions(ship)
diff --git a/src/components/command/ImpactCard.tsx b/src/components/command/ImpactCard.tsx
index 355702f..00d3cb7 100644
--- a/src/components/command/ImpactCard.tsx
+++ b/src/components/command/ImpactCard.tsx
@@ -1,7 +1,8 @@
-import { fleet } from "@/data/fleet";
+import { useFleet } from "@/context/FleetContext";
 import { calculateImpact } from "@/engine/impactEngine";
 
 export default function ImpactCard() {
+  const { fleet } = useFleet();
 
   const impacts = fleet.flatMap(ship =>
     calculateImpact(ship)
diff --git a/src/components/command/MissionOverviewCard.tsx b/src/components/command/MissionOverviewCard.tsx
index 0e32711..b3627b6 100644
--- a/src/components/command/MissionOverviewCard.tsx
+++ b/src/components/command/MissionOverviewCard.tsx
@@ -1,7 +1,8 @@
-import { fleet } from "@/data/fleet";
+import { useFleet } from "@/context/FleetContext";
 import { calculateMission } from "@/engine/MissionEngine";
 
 export default function MissionOverviewCard() {
+  const { fleet } = useFleet();
 
   const all = fleet.flatMap((ship) => calculateMission(ship));
 
diff --git a/src/components/command/PriorityFleetCard.tsx b/src/components/command/PriorityFleetCard.tsx
index e33d25b..bd1b1a9 100644
--- a/src/components/command/PriorityFleetCard.tsx
+++ b/src/components/command/PriorityFleetCard.tsx
@@ -1,7 +1,8 @@
-import { fleet } from "@/data/fleet";
+import { useFleet } from "@/context/FleetContext";
 import { calculateReadiness } from "@/engine/calculateReadiness";
 
 export default function PriorityFleetCard() {
+  const { fleet } = useFleet();
 
   const ships = fleet
     .map((ship) => ({
diff --git a/src/components/command/cards/CommanderBrief.tsx b/src/components/command/cards/CommanderBrief.tsx
index b144431..21fb1bc 100644
--- a/src/components/command/cards/CommanderBrief.tsx
+++ b/src/components/command/cards/CommanderBrief.tsx
@@ -1,34 +1,7 @@
-import { fleet } from "@/data/fleet";
-import { calculateReadiness } from "@/engine/calculateReadiness";
+import { useCommanderSnapshot } from "@/hooks/useCommanderSnapshot";
 
 export default function CommanderBrief() {
-
-  const assessments =
-    fleet.map((ship) => ({
-      ship,
-      result: calculateReadiness(ship),
-    }));
-
-  const ready =
-    assessments.filter(
-      (a) => a.result.readiness === "Y"
-    ).length;
-
-  const limited =
-    assessments.filter(
-      (a) => a.result.readiness === "Q"
-    ).length;
-
-  const notReady =
-    assessments.filter(
-      (a) => a.result.readiness === "N"
-    ).length;
-
-  const avg =
-    assessments.reduce(
-      (sum, a) => sum + a.result.score,
-      0
-    ) / assessments.length;
+  const { ready, limited, notReady, average } = useCommanderSnapshot();
 
   return (
 
@@ -58,7 +31,7 @@ export default function CommanderBrief() {
         โดยมีคะแนนความพร้อมเฉลี่ย
 
         <span className="font-bold text-sky-400">
-          {" "}{avg.toFixed(1)}%
+          {" "}{average.toFixed(1)}%
         </span>
 
       </p>
diff --git a/src/components/command/cards/CommanderExecutiveBrief.tsx b/src/components/command/cards/CommanderExecutiveBrief.tsx
index f4d427e..2e19a35 100644
--- a/src/components/command/cards/CommanderExecutiveBrief.tsx
+++ b/src/components/command/cards/CommanderExecutiveBrief.tsx
@@ -1,24 +1,7 @@
-import { fleet } from "@/data/fleet";
-import { calculateReadiness } from "@/engine/calculateReadiness";
+import { useCommanderSnapshot } from "@/hooks/useCommanderSnapshot";
 
 export default function CommanderExecutiveBrief() {
-
-  const result = fleet.map(calculateReadiness);
-
-  const ready =
-    result.filter(r => r.readiness === "Y").length;
-
-  const limited =
-    result.filter(r => r.readiness === "Q").length;
-
-  const notReady =
-    result.filter(r => r.readiness === "N").length;
-
-  const average =
-    result.reduce(
-      (sum, r) => sum + r.score,
-      0
-    ) / result.length;
+  const { ready, limited, notReady, average } = useCommanderSnapshot();
 
   return (
 
diff --git a/src/components/command/cards/CommanderStatusBar.tsx b/src/components/command/cards/CommanderStatusBar.tsx
index 22d0e76..89de16c 100644
--- a/src/components/command/cards/CommanderStatusBar.tsx
+++ b/src/components/command/cards/CommanderStatusBar.tsx
@@ -1,59 +1,14 @@
-import { fleet } from "@/data/fleet";
-import { calculateReadiness } from "@/engine/calculateReadiness";
+import { useCommanderSnapshot } from "@/hooks/useCommanderSnapshot";
 
 export default function CommanderStatusBar() {
- const results = fleet.map((ship) => {
-  try {
-    return calculateReadiness(ship);
-  } catch (e) {
-    console.error(e);
-    return {
-      readiness: "N",
-      score: 0,
-      personnel: 0,
-      equipment: 0,
-    };
-  }
-});
-
-  const ready = results.filter((r) => r.readiness === "Y").length;
-  const limited = results.filter((r) => r.readiness === "Q").length;
-  const notReady = results.filter((r) => r.readiness === "N").length;
-
-  const avg =
-    results.reduce((sum, r) => sum + r.score, 0) / results.length;
+  const { ready, limited, notReady, average } = useCommanderSnapshot();
 
   return (
     <div className="grid gap-4 md:grid-cols-4">
-
-      <Status
-        title="พร้อมปฏิบัติ"
-        subtitle="Ready"
-        value={ready}
-        color="text-emerald-400"
-      />
-
-      <Status
-        title="พร้อมบางส่วน"
-        subtitle="Qualified"
-        value={limited}
-        color="text-yellow-400"
-      />
-
-      <Status
-        title="ไม่พร้อม"
-        subtitle="Not Ready"
-        value={notReady}
-        color="text-red-400"
-      />
-
-      <Status
-        title="คะแนนเฉลี่ย"
-        subtitle="Average Score"
-        value={`${avg.toFixed(1)}%`}
-        color="text-sky-400"
-      />
-
+      <Status title="พร้อมปฏิบัติ" subtitle="Ready" value={ready} color="text-emerald-400" />
+      <Status title="พร้อมบางส่วน" subtitle="Qualified" value={limited} color="text-yellow-400" />
+      <Status title="ไม่พร้อม" subtitle="Not Ready" value={notReady} color="text-red-400" />
+      <Status title="คะแนนเฉลี่ย" subtitle="Average Score" value={`${average.toFixed(1)}%`} color="text-sky-400" />
     </div>
   );
 }
@@ -71,19 +26,9 @@ function Status({
 }) {
   return (
     <div className="rounded-2xl border border-slate-800 bg-slate-950/70 p-5">
-
-      <p className="text-sm font-semibold text-white">
-        {title}
-      </p>
-
-      <p className="text-xs uppercase tracking-wider text-slate-500">
-        {subtitle}
-      </p>
-
-      <p className={`mt-3 text-4xl font-bold ${color}`}>
-        {value}
-      </p>
-
+      <p className="text-sm font-semibold text-white">{title}</p>
+      <p className="text-xs uppercase tracking-wider text-slate-500">{subtitle}</p>
+      <p className={`mt-3 text-4xl font-bold ${color}`}>{value}</p>
     </div>
   );
-}
\ No newline at end of file
+}
diff --git a/src/hooks/useCommanderSnapshot.ts b/src/hooks/useCommanderSnapshot.ts
new file mode 100644
index 0000000..2e72814
--- /dev/null
+++ b/src/hooks/useCommanderSnapshot.ts
@@ -0,0 +1,36 @@
+import { useFleet } from "@/context/FleetContext";
+import { calculateReadiness } from "@/engine/calculateReadiness";
+
+export function useCommanderSnapshot() {
+  const { fleet } = useFleet();
+
+  const results = fleet.map((ship) => {
+    try {
+      return calculateReadiness(ship);
+    } catch (error) {
+      console.error(`Unable to calculate readiness for ${ship.shipName}`, error);
+      return {
+        readiness: "N" as const,
+        score: 0,
+        personnel: 0,
+        equipment: 0,
+      };
+    }
+  });
+
+  const ready = results.filter((result) => result.readiness === "Y").length;
+  const limited = results.filter((result) => result.readiness === "Q").length;
+  const notReady = results.filter((result) => result.readiness === "N").length;
+  const total = results.length;
+  const average = total > 0
+    ? results.reduce((sum, result) => sum + result.score, 0) / total
+    : 0;
+
+  return {
+    ready,
+    limited,
+    notReady,
+    total,
+    average,
+  };
+}
diff --git a/src/pages/CommandCenter/CommandCenterPage.tsx b/src/pages/CommandCenter/CommandCenterPage.tsx
index e2811bd..8b32960 100644
--- a/src/pages/CommandCenter/CommandCenterPage.tsx
+++ b/src/pages/CommandCenter/CommandCenterPage.tsx
@@ -1,4 +1,5 @@
 import { MainLayout } from "@/components/layout/MainLayout";
+import { useCommanderSnapshot } from "@/hooks/useCommanderSnapshot";
 
 import SecurityBanner from "@/components/common/SecurityBanner";
 import OperationalMap from "@/components/command/OperationalMap";
@@ -10,6 +11,8 @@ import CommanderBrief from "@/components/command/cards/CommanderBrief";
 import CommandGrid from "@/components/command/layout/CommandGrid";
 
 export default function CommandCenterPage() {
+  const { average, total } = useCommanderSnapshot();
+
   return (
     <MainLayout>
       <div className="mx-auto max-w-[1800px] space-y-6">
@@ -48,11 +51,11 @@ export default function CommandCenterPage() {
               <div className="mt-2 flex items-end gap-3">
 
                 <span className="text-5xl font-bold text-emerald-400">
-                  96%
+                  {average.toFixed(1)}%
                 </span>
 
                 <span className="pb-2 text-slate-400">
-                  ระดับความพร้อม
+                  ระดับความพร้อม · {total} ลำ
                 </span>
 
               </div>
