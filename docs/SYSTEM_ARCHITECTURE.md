# System Architecture

## Overview
The CRIS GYF application is structured as a React frontend with a Vite build system, Tailwind CSS styling, and reusable UI components from Shadcn.

## Key Layers
- Presentation: React pages and layout components
- Data: mock data modules and service abstractions
- UI: shared component library and design tokens
- Infrastructure: Vite dev server and build pipeline
